import { Model } from '@lk-framework/boilerplate/model/Model';
{additionalImport}

export class {modelName} extends Model {
    static type: {modelType} = '{modelTypeValue}';

{properties}

    constructor(config: any) {
        super(config);

        this.collectAttributes(config);
    }
}
